{
    "reporter": {
        "automate" : {
            "stdout" : "{{cfg.reporter.stdout}}",
            "url" : "{{cfg.reporter.url}}",
            "token" : "{{cfg.reporter.token}}",
            "node_name" : "{{ sys.hostname }}",
            "verify_ssl" : "{{cfg.reporter.verify_ssl}}",
            "node_uuid" : "{{sys.member_id}}"
        }
    }
  }